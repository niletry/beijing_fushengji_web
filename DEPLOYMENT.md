# 🚀 Cloudflare Pages 部署指南

## 📋 部署概述

本项目是一个纯静态网站，非常适合部署到 Cloudflare Pages。部署后你将获得：

- ✅ **免费托管** - 无限流量和带宽
- ✅ **全球CDN** - 超快访问速度
- ✅ **自动HTTPS** - 免费SSL证书
- ✅ **自动部署** - Git推送后自动更新
- ✅ **自定义域名** - 支持绑定自己的域名

---

## 🌐 方法一：通过 Cloudflare Dashboard（推荐）

### 第1步：登录 Cloudflare

1. 访问 https://dash.cloudflare.com/
2. 登录你的 Cloudflare 账号
   - 如果没有账号，点击 "Sign Up" 免费注册
   - 只需要邮箱即可，无需信用卡

### 第2步：进入 Pages

1. 在左侧导航栏找到 **"Workers & Pages"**
2. 点击进入 Pages 管理页面
3. 点击 **"Create application"** 按钮
4. 选择 **"Pages"** 标签
5. 点击 **"Connect to Git"**

### 第3步：连接 GitHub

1. 选择 **"GitHub"** 作为 Git 提供商
2. 点击 **"Connect GitHub"**
3. 在弹出窗口中授权 Cloudflare 访问你的 GitHub
4. 选择授权范围：
   - **推荐**：只授权 `beijing_fushengji_web` 仓库
   - 或者：授权所有仓库（方便以后部署其他项目）

### 第4步：选择仓库

1. 在仓库列表中找到 `niletry/beijing_fushengji_web`
2. 点击 **"Begin setup"**

### 第5步：配置构建设置

填写以下配置：

```
项目名称 (Project name):
  beijing-fushengji
  （或你喜欢的名字，将成为 URL 的一部分）

生产分支 (Production branch):
  main

框架预设 (Framework preset):
  None（选择"无"）

构建命令 (Build command):
  留空（不需要构建）

构建输出目录 (Build output directory):
  /
  （根目录，因为 index.html 在根目录）

根目录 (Root directory):
  /
  （留空或填 /）
```

### 第6步：部署

1. 检查配置无误后，点击 **"Save and Deploy"**
2. Cloudflare 开始部署，通常只需 **10-30秒**
3. 部署成功后，你会看到：
   ```
   ✅ Success! Your site is live at:
   https://beijing-fushengji.pages.dev
   ```

### 第7步：访问你的网站

1. 点击提供的 URL 访问你的游戏
2. 每次你推送代码到 GitHub，Cloudflare 会自动重新部署

---

## 💻 方法二：使用 Wrangler CLI（命令行）

### 安装 Wrangler

```bash
# 使用 npm 安装
npm install -g wrangler

# 或使用 yarn
yarn global add wrangler
```

### 登录 Cloudflare

```bash
wrangler login
```

这会打开浏览器，让你授权 Wrangler 访问你的 Cloudflare 账号。

### 部署项目

```bash
# 进入项目目录
cd /Users/sam/code123/beijing_fushengji_web

# 部署到 Cloudflare Pages
wrangler pages deploy . --project-name=beijing-fushengji
```

### 后续更新

每次更新代码后，只需运行：

```bash
wrangler pages deploy .
```

---

## 🎯 部署后的配置

### 自定义域名（可选）

1. 在 Cloudflare Pages 项目页面
2. 点击 **"Custom domains"** 标签
3. 点击 **"Set up a custom domain"**
4. 输入你的域名（如 `game.yourdomain.com`）
5. 按照提示添加 DNS 记录

### 环境变量（本项目不需要）

本项目是纯静态网站，不需要环境变量。

### 访问控制（可选）

如果你想限制访问：
1. 进入项目设置
2. 找到 **"Access"** 选项
3. 可以设置密码保护或 IP 白名单

---

## 📊 部署验证清单

部署完成后，检查以下项目：

- [ ] 游戏首页正常显示
- [ ] 赛博朋克样式正确加载
- [ ] 可以开始新游戏
- [ ] 可以保存/读取游戏
- [ ] 滚动新闻栏正常工作
- [ ] 所有按钮和交互正常
- [ ] 移动端显示正常

---

## 🔧 常见问题

### Q1: 部署后样式丢失？

**原因**：CSS/JS 文件路径问题

**解决**：检查 `index.html` 中的资源引用是否使用相对路径：
```html
<!-- ✅ 正确 -->
<link rel="stylesheet" href="style.css">
<script type="module" src="js/main.js"></script>

<!-- ❌ 错误 -->
<link rel="stylesheet" href="/style.css">
```

### Q2: 游戏功能不正常？

**原因**：浏览器缓存

**解决**：
1. 清除浏览器缓存
2. 或在 URL 后加版本号：`?v=2`
3. 或使用隐私模式测试

### Q3: 如何回滚到之前的版本？

1. 在 Cloudflare Pages 项目页面
2. 点击 **"Deployments"** 标签
3. 找到之前的部署记录
4. 点击 **"Rollback"**

### Q4: 如何查看部署日志？

1. 在 Cloudflare Pages 项目页面
2. 点击最新的部署记录
3. 查看 **"Build log"**

---

## 🚀 自动部署工作流

配置完成后，你的工作流将是：

```bash
# 1. 修改代码
vim index.html

# 2. 提交到 Git
git add .
git commit -m "feat: 添加新功能"

# 3. 推送到 GitHub
git push

# 4. Cloudflare 自动部署（无需任何操作）
# 5. 30秒后，访问 https://beijing-fushengji.pages.dev 查看更新
```

---

## 📈 性能优化建议

### 启用 Cloudflare 优化

在 Cloudflare Pages 设置中：
1. **Auto Minify** - 自动压缩 HTML/CSS/JS
2. **Brotli** - 启用 Brotli 压缩
3. **HTTP/3** - 启用 HTTP/3 协议

### 缓存策略

Cloudflare 会自动为静态资源设置缓存，无需额外配置。

---

## 🎉 部署成功！

恭喜！你的《北京浮生记》现在已经部署到全球 CDN，任何人都可以访问了！

**分享你的游戏**：
- 直接分享 URL：`https://beijing-fushengji.pages.dev`
- 或绑定自定义域名后分享

**下一步**：
- 在 README.md 中添加在线演示链接
- 在 GitHub 仓库描述中添加网站链接
- 分享到社交媒体

---

## 📞 获取帮助

- **Cloudflare Pages 文档**：https://developers.cloudflare.com/pages/
- **Cloudflare 社区**：https://community.cloudflare.com/
- **GitHub Issues**：https://github.com/niletry/beijing_fushengji_web/issues

---

**部署时间**：2025-12-29  
**部署方式**：Cloudflare Pages  
**仓库地址**：https://github.com/niletry/beijing_fushengji_web
